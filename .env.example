# Environment Variables Configuration
# Copy this file to .env and fill in your actual values

# Twitter API Configuration
# Get your bearer token from https://developer.twitter.com/
TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here

# Firebase Configuration
# Option 1: Provide the entire Firebase service account JSON as a string
# Copy the contents of your serviceAccountKey.json and paste as a single line
FIREBASE_CREDENTIALS={"type":"service_account","project_id":"your-project-id",...}

# Option 2: Or just provide the path (if deploying with file)
# SERVICE_ACCOUNT_PATH=serviceAccountKey.json

# Application Configuration
PORT=8000
CACHE_HOURS=10
MAX_TWEETS_PER_CALL=15

# CORS Configuration
# For production, specify your Flutter app domains (comma-separated)
# Example: ALLOWED_ORIGINS=https://your-app.com,https://another-domain.com
ALLOWED_ORIGINS=*

# Database Configuration (optional)
DB_PATH=sentiment.db
